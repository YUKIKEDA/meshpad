[package]
name = "meshpad"
version = "1.0.0"
edition = "2021"
description = "Lightweight STL/NAS 3D viewer"
repository = "https://github.com/YUKIKEDA/meshpad"
readme = "README.md"

[dependencies]
anyhow = "1"
bytemuck = { version = "1", features = ["derive"] }
eframe = { version = "0.32", default-features = false, features = ["wgpu", "default_fonts"] }
egui = "0.32"
fast-float2 = "0.2"
glam = { version = "0.30", features = ["bytemuck"] }
image = { version = "0.25", default-features = false, features = ["png"] }
memmap2 = "0.9"
rayon = "1.11"
rfd = "0.17"
rustc-hash = "2"
wgpu = { version = "25", default-features = true }

[target.'cfg(windows)'.build-dependencies]
winresource = "0.1"

[dev-dependencies]
pollster = "0.4"
stl_io = "0.11"

[[bench]]
name = "stl_parse"
path = "bench/stl_parse.rs"
harness = false

[[bench]]
name = "nas_parse"
path = "bench/nas_parse.rs"
harness = false

[profile.release]
lto = true
codegen-units = 1
